# Empty by default: Vite proxies /api requests to http://localhost:8080 in development.
# For a separately deployed API, set the full API origin (without a trailing slash).
# VITE_API_BASE_URL=https://api.example.com
